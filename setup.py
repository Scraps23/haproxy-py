# coding: utf-8

"""
    HAProxy Data Plane API

    API for editing and managing haproxy instances. Provides process information, configuration management, haproxy stats and logs.   # noqa: E501

    OpenAPI spec version: 2.7
    Contact: support@haproxy.com
    Generated by: https://github.com/swagger-api/swagger-codegen.git
"""

from setuptools import setup, find_packages, re  # noqa: H301

NAME = "haproxy-py"


def read(filename):
    with open(filename, encoding='utf8', errors='ignore') as file:
        return "\n" + file.read()


try:
    import pypandoc
    long_description = pypandoc.convert_file('README.md', 'rst')
except ModuleNotFoundError:
    long_description = read('README.md')


def get_version() -> str:
    """Get the version string from the module's __init__ file."""
    with open("dataplaneapi/__init__.py") as init:
        version = re.search(r'[\'"]\S\d*.\d*.\d*\S*[\'"]', init.read())
        return version.group().strip('"').strip("'")


REQUIRES = ["urllib3 >= 1.15", "six >= 1.10", "certifi", "python-dateutil"]

setup(
    name=NAME,
    version=get_version(),
    description="HAProxy Data Plane API",
    author_email="support@haproxy.com",
    url="",
    keywords=["Swagger", "HAProxy Data Plane API"],
    install_requires=REQUIRES,
    packages=find_packages(),
    include_package_data=True,
    long_description=long_description
)
